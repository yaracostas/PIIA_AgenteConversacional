services:
  turn-server:
    build: ./server
    container_name: turn-server
    ports:
      - "8770:8770"
    environment:
      GROQ_API_KEY: ${GROQ_API_KEY}
      USE_GPU: "false"
      TTS_PITCH_FACTOR: "1.45"
    restart: unless-stopped